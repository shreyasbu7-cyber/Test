pipeline {
    agent any
    stages {
        stage("clone") {
            steps {
                git 'https://github.com/shreyasbu7-cyber/Test.git'
            }
        }
        stage("build") {
            steps {
               sh "mvn clean install"
            }
        }
    }
}
